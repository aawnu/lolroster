{
  "scripts": {
    "server": "ts-node --transpile-only server",
    "build": "npm ci && ts-node --transpile-only src/download",
    "download": "ts-node --transpile-only src/download",
    "test": "ts-node --transpile-only test/validate-endpoints",
    "dev": "tsnd --respawn --clear --watch 'src/**' server"
  },
  "dependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^18.0.0",
    "axios": "^0.26.1",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "jimp": "^0.16.1",
    "jquery": "^3.6.0",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
